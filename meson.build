project('meson-applet-template', 'c',
  version : '0.0.1',
  default_options : ['warning_level=3'])

elf2bestape = find_program('elf2bestape', required: false)
romtool = find_program('romtool', required: false)

subdir('src')
